# Optional deps for --mobile_deploy (TFLite FP32 + INT8). Not required for the core pipeline.
#
# Minimal nn-lite conversion stack (see torch2tflite.py — no PyPI "tensorflow").
# ai_edge_tensorflow provides `import tensorflow as tf` for INT8 converter flags.
# https://github.com/ABrain-One/nn-lite/blob/main/requirements.txt
#
# Do not pin old nightly dates (e.g. litert 20260202) — those wheels expire on PyPI.
# litert/quantizer are pulled in transitively by ai-edge-torch if needed.

ai-edge-torch-nightly==0.8.0.dev20260122
ai_edge_tensorflow==2.21.0.dev20251110
